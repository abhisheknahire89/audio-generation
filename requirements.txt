# OPD Audio Generator — Python dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.0.0

# Database
# (sqlite3 is stdlib)

# Audio processing
numpy>=1.24.0
soundfile>=0.12.1
pydub>=0.25.1
librosa>=0.10.0

# TTS — Indic-Parler-TTS
# parler-tts must be installed from git:
# pip install git+https://github.com/huggingface/parler-tts.git
transformers>=4.40.0
accelerate>=0.29.0
torch>=2.1.0

# TTS — IndicF5 (optional, install separately)
# pip install git+https://github.com/ai4bharat/IndicF5.git

# Script parsing
PyMuPDF>=1.24.0        # for PDF parsing (fitz)
python-docx>=1.1.0     # for DOCX parsing

# Utilities
httpx>=0.27.0
aiofiles>=23.2.1
